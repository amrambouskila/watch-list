/** One freely-licensed image Claude found, offered as artwork for a category card. */
export interface HeroCandidate {
  url: string;
  source_file: string;
  licence: string;
  page: string;
  description: string;
}
